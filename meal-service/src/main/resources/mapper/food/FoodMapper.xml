<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.example.mealservice.domain.food.persistence.FoodMapper">
    <!--  음식 단건 조회  -->
    <select id="findById" parameterType="long" resultType="FoodItem">
        SELECT food_item_id,
               meal_record_id,
               food_name,
               amount,
               nutrient_type,
               source_type
        FROM food_item
        WHERE food_item_id = #{foodItemId}
    </select>

    <!--  음식 수정  -->
    <update id="updateFoodItem" parameterType="FoodItem">
        UPDATE food_item
        SET
            food_name = #{foodItem.foodName},
            amount = #{foodItem.amount},
            nutrient_type = #{foodItem.nutrientType},
            source_type = #{foodItem.sourceType}
        WHERE food_item_id = #{foodItem.foodItemId}
    </update>

    <!--  존재 여부 확인  -->
    <select id="existsByIdAndMealId" parameterType="map" resultType="boolean">
        SELECT COUNT(1) > 0
        FROM food_item
        WHERE food_item_id = #{foodItemId}
        AND meal_record_id = #{mealRecordId}
    </select>
</mapper>